Place `Outfit-Medium.otf` in this folder.

The app loads it as `fontFamily: 'OutfitMedium'` (see `src/constants/fonts.ts`).

Used on: homepage deck index, Current card hero values, explore-v2 timers.
