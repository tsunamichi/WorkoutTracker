# Frozen React Native migration inputs

These anonymized, manually curated JSON values capture representative legacy shapes for a future importer. They are input evidence only: Phase 0 does not decode or insert them into SwiftData.

Excluded intentionally: authentication/session tokens, API keys, photos, body weight, HIIT, bonus/core data, and trainer conversations. The React Native source and persisted store are unchanged.
